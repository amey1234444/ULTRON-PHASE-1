import type {
  AppSettings,
  BackendStatus,
  DeviceInfo,
  DiscoveryProgressEvent,
  ModbusSensorReading,
  ModbusTarget,
} from '@ultron/hmi-core';

export type { AppSettings, BackendStatus, DiscoveryProgressEvent, ModbusSensorReading, ModbusTarget };
export type TauriDeviceInfo = DeviceInfo;
